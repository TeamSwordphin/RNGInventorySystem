--[[
	Default item data template (which each item in the player's inventory will use). Editing keys or values will NOT automatically update player saves.
	You must reconcile this manually. So please make sure to ensure this template is more or less final or can withstand
	changes.
]]

return {
	Count = 1, --- Starts off at 1 count at initialization
}
